#include <assert.h>
#include <stdio.h>
#include "bclib.h"

int main(int argc, char **argv)
{
  int barcode[80][BC_NBITS] = {0};
  int i,j;
  int bits[BC_NBITS];
  
  for(i = 1, j = 0; i < argc; ++i)
  {
      barcode[i][j] =   bc_char2bits();
  }
  
  /* print */
  for(){
    for(i = 0; i < BC_NBITS; ++i)
    {
      for(j = 0;j < ; ++j)
      {
	if(barcode[][] == 1)
	  printf("#");
	  
	else
	  printf(" ");
	
      }
    }
  }
  
  
}

